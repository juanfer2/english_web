export interface StyleColors {
  primary: string;
  primaryRGBA: string;
  primaryLight: string;
  primaryGradientDiagonal: string;
  primaryGradient: string;
  contrastPrimary: string;
  secondary: string;
  ternary: string;
}

export interface StyleTheme {
  colors: StyleColors;
}
