import { render } from '@testing-library/react';
import React from 'react';

import GerunsAndInfinitives from './index';

const Props = {};

beforeEach(() => {
  render(<GerunsAndInfinitives {...Props} />);
});

test('Its rendering', () => {});
